#!/usr/bin/python3
import random
number = random.randint(-10000, 10000)
if number > 5:
    last_num = number % 10
    spec_str = "greater than 5"
elif number < 0
print("Last digit of {:d} is {:d} and is {:s}".format(number, )
